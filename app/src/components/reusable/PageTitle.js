import styled from 'styled-components';
import { customBlack, fontTitle } from '../Variables';

const PageTitle = styled.h1`
  color: ${customBlack};
  font-family: ${fontTitle};
  padding: 15px;
  margin: 0;
`;

export default PageTitle;